server:
  port: 8002
spring:
  application:
    name: qlpro-comsumer03-8002-consul
  cloud:
    consul:
      # consul注册中心地址
      host: localhost
      port: 8500
      discovery:
        #hostname: 127.0.0.1
        service-name: ${spring.application.name}